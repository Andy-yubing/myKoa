<% include header %>
<div class="container">
		<form class="form create" method="post ">
			<div>
				<label>用户名：</label> 
				<input placeholder="用户名" type="text" name="name">
			</div>
			<div>
				<label>密码：</label> 
				<input placeholder="密码" type="password" name="password">
			</div>
			<div class="submit">登录</div>
		</form>		
</div>
    
<% include footer %>
<script>
    $(".submit").on("click",function(){
        if($('input[name=name]').val().trim() == '' || $('input[name=password]').val().trim() == '' ){
            fade("输入为空");
        }else{
            $.ajax({
                url: "/signin",
                data: $(".form").serialize(),
                type: 'post',
                dataType: 'json',
                success(result){
                    console.log(result)
                    if(!result){
                        alert("密码错误");
                    }else{
                        window.locahost.href = "/posts";
                    }
                },
                err(err){
                    console.log(err)
                }
            })
        }
    })
</script>
